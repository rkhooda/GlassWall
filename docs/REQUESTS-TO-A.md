# REQUESTS-TO-A — Append-Only Log

**Format:** `Date | File | What I Need | Why | Blocks What`

---

2026-08-29 | (none) | No new manifest/CSP changes from inference spike | Offscreen document direct placement works; model loads via `chrome.runtime.getURL()`; no `connect-src` or `worker-src` changes needed | —

*No other entries.*2026-09-01 | `apps/extension/manifest.json` | `content_security_policy.extension_pages` must allow `'wasm-unsafe-eval'` | P8/P9 both run WASM: onnxruntime-web for NER and the tesseract core for OCR. MV3 blocks WebAssembly compilation on extension pages without this directive. No `connect-src` change is needed and none is wanted — every asset loads through `chrome.runtime.getURL()`. | Offscreen NER and OCR cannot run in a real browser without it. Unit tests pass regardless, so this will not be caught by CI.

2026-09-01 | `packages/schema/tsconfig.json` | Drop `emitDeclarationOnly: true` so the package emits JS | `packages/schema` exports runtime zod values (`SanitizedObservationSchema`, used by `egressGate`), but `dist/` contains only `.d.ts`. Anything importing schema at runtime from `dist` fails to resolve. Worked around for now by resolving workspace packages from source in `apps/extension/vite.config.ts`; the underlying package is still unbuildable as a runtime dependency. | Any consumer of `@glasswall/schema` that is not bundled by vite — including a plain node script over `dist`.

2026-09-01 | `packages/schema/src/policy.ts` (`PiiTypeSchema`) | Proposal: add `AADHAAR`, `PAN`, `IFSC`, `GSTIN`, `UPI`, `MRN`, `POSTAL_CODE` | The P6-a recognizers detect these India-specific types, but the frozen enum cannot name them, so `sanitize()` folds them into `PERSONAL`/`FINANCIAL`/`HEALTH` (see `RECOGNIZER_TO_PII` in `packages/privacy/src/sanitize.ts`). The audit record therefore cannot say *which* identifier was redacted, which weakens both the inspector and the leakage report. Not urgent — the redaction itself is correct either way. | Per-type leakage reporting in P12-B; the demo inspector showing "AADHAAR withheld" rather than "PERSONAL withheld".

2026-09-01 | `apps/extension/src/background/orchestrator.ts` | Pass the perception sources into `sanitize()` | P8/P9 are built, tested and exported as `perceptionSources` from `apps/extension/src/offscreen/pipeline/index.ts`, but the orchestrator's `sanitize()` call omits the argument, so NER and OCR never run in the live step loop. One line: `perceptionSources` in the call at orchestrator.ts:198. The `degraded[]` handling already there (`recordFailure('ocr' | 'ner')`) will start receiving real values. Also worth passing the captured frame rather than `frame: null` — with a null frame OCR masks every unexplained region instead of reading it. | P8, P9 and the D6 OCR skip-rate claim in the live demo.

2026-09-01 | `pnpm-workspace.yaml` | Change the `eval/*` pattern to `eval` | `@glasswall/eval` sits at `eval/package.json`, which `eval/*` does not match, so it is not a workspace member: `pnpm ls -r` does not list it, its `workspace:*` deps are unlinked, and `turbo run test` never runs it. The P11 ablation (`eval/ablations/fusion.test.ts`) and the P8 detection metrics therefore pass locally under `cd eval && pnpm exec vitest run` and are invisible to CI. Cross-package imports currently resolve through a `resolve.alias` block I added to `eval/vitest.config.ts` and through the `@glasswall/<pkg>/*` mappings in `tsconfig.base.json` — both workarounds for the same missing membership. | Any acceptance number measured in `eval/` being enforced by CI rather than by hand.

2026-09-01 | `apps/bench-site/src/sites/shoplite/**` | `pnpm build` fails on `main` | `@glasswall/bench-site#build` exits 1 with ~20 errors in `shoplite/pages/{CheckoutPage,OrdersPage}.tsx` — `autocomplete` should be `autoComplete` on a React input, and `order` is possibly undefined under `noUncheckedIndexedAccess`. Present before this branch (verified by stashing). Not my lane, so untouched. Worth noting that the `autocomplete` typo also means those inputs carry no autocomplete attribute at runtime, which is exactly the signal `element-rules.ts` classifies on — so ShopLite is currently under-detecting by accident. | A green `pnpm build`, and honest element-rule detection numbers on ShopLite.

2026-09-01 | `apps/extension/src/sidepanel/App.tsx` | Mount the privacy inspector. Two lines, exactly:<br>import — `import { Inspector } from './privacy';`<br>JSX (inside `panel-body`, after `<Trace .../>`) — `<Inspector raw={inspect?.raw ?? null} payload={inspect?.payload ?? null} redactions={inspect?.redactions ?? []} degraded={inspect?.degraded ?? []} />`<br>plus the state it reads: a `const [inspect, setInspect] = useState<{ raw: RawObservation; payload: SanitizedObservation; redactions: RedactionReason[]; degraded: string[] } | null>(null)` fed from a new `extension:privacy-inspect` message the orchestrator posts per step with `{ raw, observation, redactions, degraded }` straight off the `SanitizeResult`. | `Inspector.tsx` and `Handles.tsx` are built, tested and exported from `apps/extension/src/sidepanel/privacy/index.ts`, but `App.tsx` is A's file and I do not edit it. The inspector is PLAN.md §28 cut #11 — the one piece of UI that survives every cut — and it renders nothing without a mount. | The judge demo: naming a secret and showing it is absent from the outbound payload.

2026-09-01 | `apps/extension/src/background/orchestrator.ts` | The `extension:privacy-inspect` message above must carry the **raw** observation. | The inspector's argument is the side-by-side: local raw on the left, outbound payload on the right. Raw stays inside the extension — side panel only, over `chrome.runtime` messaging, never into `net.ts`, never into the audit record, never persisted. `assertNoValuesInAudit()` still applies to the audit path and is unaffected. If you would rather not move raw across that boundary at all, say so and I will render the left pane from a redacted-but-unbuilt projection instead — it is a weaker demo and I would rather not. | The left pane of the inspector.

2026-09-01 | `apps/extension/src/background/index.ts` | Call the session warm-up on install. One listener:<br>`chrome.runtime.onInstalled.addListener(() => { void warmUpInference(PROFILES[defaultProfile].policy); });`<br>importing `warmUpInference` from `@glasswall/inference` and `PROFILES` from `@glasswall/privacy`. It never throws — every failure is swallowed and reported in the returned `WarmupReport`. | Measured on the M1/8GB in `eval/reports/perf.md`: a step that finds the NER model cold pays ~500ms (148ms import + 300ms load + 20ms first inference). Warm-up moves ~97% of that off the user's first action. It also decides *what* loads: under STRICT the policy disables the screenshot, so the 43MB OCR engine is never touched (103.9MB loaded vs 144.9MB). Nothing is made faster; it is moved off the path where a human is waiting. | The first step of every session feeling instant instead of half a second late.

2026-09-01 | `apps/extension/src/background/orchestrator.ts` | Frame ownership, when you wire `frame` into `sanitize()`. `decodeBitmap()` in the image pipeline **closes** the `ImageBitmap` it is given; OCR's `renderCrops()` only borrows it and deliberately does not close. So the screenshot pipeline must run **after** perception, or be given its own bitmap. Closing it twice throws; closing it before OCR degrades that step to `ocr_unavailable` (fail-closed, but a silent utility loss). | My lane creates no `ImageBitmap`s — every one that enters it comes from your `capture.ts` — so the lifetime is yours to sequence. There is exactly one close in the codebase (`image/decode.ts:44`) and it should stay that way. | A frame leak across steps, or an OCR path that silently stops reading anything.
